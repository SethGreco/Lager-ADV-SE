import React, {Component} from 'react';
import ReactDOM from 'react-dom';

const Printable = () => {
  return (
    <div>This will be were we create 1 page mock up for the customer</div>
  )
};

export default Printable;